{
  "name": "voxglide-server",
  "version": "1.1.2",
  "private": true,
  "type": "module",
  "description": "Vendored voxglide proxy (WebSocket + LLM key holder + SDK static server) for the voice-typing playground",
  "scripts": {
    "dev": "tsx watch --env-file=.env server/index.ts",
    "start": "tsx --env-file=.env server/index.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.0",
    "@google/genai": "^1.45.0",
    "openai": "^6.29.0",
    "tsx": "^4.0.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.0"
  }
}
